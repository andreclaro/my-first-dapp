{
	"name": "my-first-dapp",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"build": "npx hardhat compile",
		"deploy-testnet": "npx hardhat run ./scripts/MyFirstContract_deploy.js --network goerli",
		"deploy": "npx hardhat run ./scripts/MyFirstContract_deploy.js --network ethereum"
	},
	"dependencies": {
		"dotenv": "^16.0.2",
		"@openzeppelin/contracts": "^4.7.3"
	},
	"devDependencies": {
		"@nomicfoundation/hardhat-toolbox": "^1.0.2",
		"hardhat": "^2.11.1"
	}
}